const sum = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const square = (a) => a * a;
const squareRoot = (a) => Math.sqrt(a);

module.exports = {
  sum,
  subtract,
  multiply,
  divide,
  square,
  squareRoot,
};
